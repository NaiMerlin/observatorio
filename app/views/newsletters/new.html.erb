<h1>Nuevo boletín</h1>

<% if user_signed_in? -%>
	<%= render 'form' %>

	<%= link_to 'Atrás', newsletters_path %>
<%else%>
	<p> Lo sentimos solo usuarios registrados pueden crear boletines. </p>
	<%= link_to 'Atrás', newsletters_path %>
<%end%>
